/**
 * Game constants — leaderboard IDs, etc.
 *
 * Register these IDs in App Store Connect > Game Center > Leaderboards.
 */

export const LEADERBOARD_IDS = {
  classic: 'blockzen_classic',
  daily: 'blockzen_daily',
} as const;
